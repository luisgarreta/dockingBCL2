dt = read.csv ("out-rotALL.csv")
newcol = sprintf ("%s-%s", dt$TYPE, dt$VARIABLE)
head (newcol)
tail (newcol)
ndt = data.frame (dt$FRAME, VARIABLE=newcol, dt$DEGREES)
head (ndt)
ndt = data.frame (dt[,"FRAME"], VARIABLE=newcol, dt$DEGREES)
head (ndt)
ndt = cbind (dt$FRAME, VARIABLE=newcol, dt$DEGREES)
head (ndt)
ndt = data.frame (FRAME=dt$FRAME, VARIABLE=newcol, DEGREES=dt$DEGREES)
head (ndt)
write.csv (ndt, "rotations-mixed-ALL.csv", row.names=F)
exit
